# Secrets: never commit an env file, anywhere in the repo
.env
.env.*
!.env.example
!.env.replay

# real-world procurement documents (fetched by docs/realworld/fetch.md; only extracted text is committed)
docs/realworld/raw/
