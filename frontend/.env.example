# Leave unset to run on the mock results (backend-shaped, authored for the four
# samples) — no backend needed. Set it to use a live backend: /api goes through
# the Vite dev proxy (→ localhost:8000) or nginx in the compose stack.
# VITE_API_URL=/api
