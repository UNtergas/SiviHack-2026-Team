server {
    listen 80;
    server_name _;

    # API — strip the /api prefix so FastAPI sees /score, /health
    location /api/ {
        client_max_body_size 50m;         # PDF uploads for /api/documents/convert
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 600s;          # LLM calls can be slow on local 7B
        proxy_send_timeout 600s;
        proxy_buffering off;              # SSE (/api/score/stream): forward events as they come
        proxy_cache off;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }

    # Frontend — everything else
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
